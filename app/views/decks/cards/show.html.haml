%p#notice= notice

%p
  %b Question:
  = @card.question
%p
  %b Description:
  = @card.description
%p
  %b Category:

-# .cards-container
-#   = simple_form_for Card.new, url: card_cards_path(card_id: @card.id) do |f|
-#     = f.input :question, label: false, placeholder: 'question', as: :string, input_html: { autocomplete: :off }
-#     = f.input :description, label: false, placeholder: 'description', as: :string, input_html: { autocomplete: :off }

-# = link_to 'Add Card', new_card_card_path(@card)
-# \|
= link_to 'Edit', edit_deck_card_path(@card)
\|
= link_to 'Back', deck_cards_path
